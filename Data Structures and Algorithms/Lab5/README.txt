	Fifth lab for Data Structures and Algorithms. We made a hash table, 
pretty much. It can take any data type for its keys and values, and places 
them in the table accordingly. Everything is nicely retrievable and actually 
seems to function as intended. All the funky stuff that would normally break 
things is accounted for as well. Overall, jolly good lab.
	Oh yeah, to run the unit tests, run Test.java.
	Files included:
		- README.txt (this)
		- UR_HashTable (the provided class I implemented)
		- HashTable.java (the hash table implementation)
		- Test.java (my test class)